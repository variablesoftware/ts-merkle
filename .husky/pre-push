eval "$(nodenv init - zsh)"
#export PATH="$(nodenv root)/shims:$PATH"
#export NPM_TOKEN="$(security find-generic-password -a $USER -s SECRET_NPM_TOKEN -w 2>/dev/null || echo '')"
set -x
which nodenv
yarn lint || exit 1 # \
#&& yarn build \
#&& yarn vitest --printConsoleTrace \
#--reporter verbose \
#--exclude tests/integration/smoke.test.ts || exit 1
gitleaks dir . || exit 1
